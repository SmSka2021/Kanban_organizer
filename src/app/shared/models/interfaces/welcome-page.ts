export interface AdvantagesData {
  text: string | unknown;
  iconURL: string;
}

export interface TeamData {
  avatarURL: string;
  name: string;
  description: string;
}
export interface Video {
  title: string;
  link: string;
}
