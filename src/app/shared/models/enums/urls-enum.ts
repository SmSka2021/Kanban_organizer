export enum UrlsEnum {
  baseURL = 'https://vast-sea-12557.herokuapp.com/',
  users = 'users',
  logIn = 'signin',
  signUp = 'signup',
  boards = 'boards',
  columns = 'columns',
  tasks = 'tasks',
  file = 'file',
}
